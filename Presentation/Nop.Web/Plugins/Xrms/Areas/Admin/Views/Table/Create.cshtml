@model TableDetailsPageViewModel
@{
    Layout = "_AdminLayout";

    //page title
    ViewBag.Title = T("Xrms.Admin.Catalog.Tables.Details.Create.Title").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Tables");
}

<form asp-controller="Table" asp-action="Create" method="post" id="table-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Xrms.Admin.Catalog.Tables.Details.Create.Title")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Xrms.Admin.Catalog.Tables.Details.Buttons.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" id="save-table" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = "admin_table_details_buttons" })
        </div>
    </div>
    @await Html.PartialAsync("~/Plugins/Xrms/Areas/Admin/Views/Table/_CreateOrUpdate.cshtml", Model)
    <!--<script type="text/javascript">
            //delete selected
            $('#save-table').click(function(e) {
                e.preventDefault();

                var postData = {
                    Name: $('#@Html.IdFor(model => model.Name)').val(),
                    Description: $('#@Html.IdFor(model => model.Description)').val(),
                    DisplayOrder: $('#@Html.IdFor(model => model.DisplayOrder)').val(),
                    PictureId: $('#@Html.IdFor(model => model.PictureId)').val()
                };
                addAntiForgeryToken(postData);

                $.ajax({
                    cache: false,
                    type: "POST",
                    url: "@(Url.Action("Create", "Table"))",
                    data: postData,
                    error: function(xhr, ajaxOptions, thrownError) {
                        alert(thrownError);
                    },
                    traditional: true
                });
                return false;
            });
    </script>-->

</form>

